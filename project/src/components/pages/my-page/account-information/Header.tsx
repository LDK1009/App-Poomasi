import { Typography } from "@mui/material";

const Header = () => {
  return (
    <div>
      <Typography variant="h6" fontWeight={"bold"}>
        계정 정보
      </Typography>
    </div>
  );
};

export default Header;
